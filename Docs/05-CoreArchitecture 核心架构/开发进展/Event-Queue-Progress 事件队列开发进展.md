# 事件队列系统开发进展

**版本**: v1.1  
**创建日期**: 2025-01-08  
**更新日期**: 2025-01-08  
**状态**: 设计完成（双模式），待实现  

> 📖 **相关文档**：
> - [事件队列系统设计](../事件/Event-Queue-System%20事件队列系统.md)
> - [受击与击退开发进展](../../02-CombatSystem%20战斗系统/_status%20开发进展/Hit-Reaction-Progress%20受击与击退开发进展.md)

---

## TL;DR

**目标**：为 Logic 层实体间通信引入新的**双模式**事件队列机制，采用静态声明 + 预处理 + 集中调度架构，与现有 EventSystem 共存。

**核心特性**：
- ✅ **面向个体事件**：存储在实体上，性能优，自动生命周期
- ✅ **面向全体事件**：全局广播，所有实体响应
- ✅ **静态声明**：Capability 类中声明处理函数
- ✅ **预处理**：注册时缓存事件映射
- ✅ **集中调度**：专门的事件处理循环

**进展**：✅ 设计完成，📋 待实现

**预估工时**：**12-16 小时**

**关键风险**：
- ⚠️ 装箱拆箱性能开销（需测试）
- ⚠️ CapabilitySystem 注册流程改动（需确认现有逻辑）

---

## 版本历史

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.1 | 2025-01-08 | 更新为双模式设计（面向个体+面向全体） | AI Assistant |
| v1.0 | 2025-01-08 | 初始版本，设计完成 | AI Assistant |

---

## 任务分解

### Phase 0: 前置准备 ⏳

**预估工时**: 1-2 小时

- [ ] **P0.1**: 阅读现有 `CapabilitySystem` 和 `Capability<T>` 代码
- [ ] **P0.2**: 确认 `Entity` 类是否为 partial，是否可扩展
- [ ] **P0.3**: 确认 `World.Update` 调用顺序和 `World.Entities` 遍历方式
- [ ] **P0.4**: 确认 `Entity.HasCapability` 和 `GetCapability` API

**验收标准**：
- 清楚理解现有 Capability 架构
- 确认集成点和改动范围
- 确认 Entity 可以添加事件队列字段

---

### Phase 1: 核心数据结构 🔨

**预估工时**: 2-3 小时

#### 1.1 EntityEvent

- [ ] **P1.1.1**: 创建 `EntityEvent` struct（简化版，不需要 TargetEntityId）
  - `Type EventType`
  - `object EventData`
  - `int Frame`

#### 1.2 Entity 扩展（面向个体事件）

- [ ] **P1.2.1**: 在 `Entity` 类中添加 `EventQueue` 字段
  - `internal Queue<EntityEvent> EventQueue { get; private set; }`
- [ ] **P1.2.2**: 添加 `HasPendingEvents` 属性
  - `public bool HasPendingEvents => EventQueue != null && EventQueue.Count > 0;`
- [ ] **P1.2.3**: 实现 `QueueEvent<T>(T eventData)` 方法
  - 延迟创建队列
  - 入队事件
- [ ] **P1.2.4**: 实现 `ClearEventQueue()` 方法

#### 1.3 GlobalEventQueue（面向全体事件）

- [ ] **P1.3.1**: 创建 `GlobalEventQueue` 类
  - `Queue<EntityEvent> _globalEvents`
- [ ] **P1.3.2**: 实现 `QueueGlobalEvent<T>(T eventData)` 方法
- [ ] **P1.3.3**: 实现 `GetEvents()` 方法（internal）
- [ ] **P1.3.4**: 实现 `ClearAll()` 方法
- [ ] **P1.3.5**: 添加 `HasPendingEvents` 属性

**验收标准**：
- Entity 可以正确存储和检索事件
- GlobalEventQueue 可以正确存储和检索全局事件
- 延迟创建机制正常工作
- 编译通过，无警告

---

### Phase 2: Capability 事件处理扩展 🔨

**预估工时**: 3-4 小时

#### 2.1 Capability<T> 基类扩展

- [ ] **P2.1.1**: 添加 `EntityEventHandler<TEvent>` 委托类型
  - **签名**：`void Handler(Entity entity, TEvent evt)`（第一个参数必须是 Entity）
- [ ] **P2.1.2**: 添加 `Dictionary<Type, Delegate> _eventHandlers` 字段
- [ ] **P2.1.3**: 添加 `protected virtual void RegisterEventHandlers()` 方法
- [ ] **P2.1.4**: 添加 `protected void RegisterEventHandler<TEvent>(handler)` 方法
- [ ] **P2.1.5**: 添加 `internal Dictionary<Type, Delegate> GetEventHandlers()` 方法

#### 2.2 示例实现（测试用）

- [ ] **P2.2.1**: 创建测试事件 `TestEntityEvent`
- [ ] **P2.2.2**: 创建测试 Capability `TestEventCapability`
  - 实现 `RegisterEventHandlers()`
  - 实现事件处理函数（第一个参数是 Entity）

**验收标准**：
- Capability 可以正确声明事件处理器
- `GetEventHandlers()` 返回正确的映射
- 处理函数签名正确（第一个参数是 Entity）
- 编译通过，无警告

---

### Phase 3: CapabilitySystem 预处理和调度 🔨

**预估工时**: 4-5 小时

#### 3.1 事件映射缓存

- [ ] **P3.1.1**: 添加 `Dictionary<(Type, Type), Delegate> _eventHandlerCache`
- [ ] **P3.1.2**: 添加 `Dictionary<Type, List<(Type, Delegate)>> _eventToHandlers`

#### 3.2 注册时预处理

- [ ] **P3.2.1**: 修改 `RegisterCapability<T>()` 方法
  - 调用 `capability.GetEventHandlers()`
  - 缓存到 `_eventHandlerCache`
  - 建立 `_eventToHandlers` 索引

#### 3.3 双模式事件处理循环

- [ ] **P3.3.1**: 实现 `ProcessEntityEvents()` 主方法
  - 调用 `ProcessTargetedEvents()`
  - 调用 `ProcessBroadcastEvents()`
  
- [ ] **P3.3.2**: 实现 `ProcessTargetedEvents()` 方法
  - 遍历 `World.Entities`
  - 检查 `entity.HasPendingEvents`
  - 出队并调用 `DispatchEventToEntity()`
  
- [ ] **P3.3.3**: 实现 `ProcessBroadcastEvents()` 方法
  - 检查 `World.GlobalEventQueue.HasPendingEvents`
  - 遍历全局事件队列
  - 对每个事件，遍历所有实体调用 `DispatchEventToEntity()`
  
- [ ] **P3.3.4**: 实现 `DispatchEventToEntity(Entity entity, EntityEvent evt)` 方法
  - 查找 `_eventToHandlers`
  - 检查实体是否有该 Capability 且激活
  - 调用 `InvokeHandler()`
  
- [ ] **P3.3.5**: 实现 `InvokeHandler(Delegate handler, Entity entity, object eventData)` 方法
  - 使用 `DynamicInvoke` 拆箱调用（第一个参数是 entity）
  - 异常处理和日志

**验收标准**：
- 个体事件能正确分发到对应实体的 Capability
- 全体事件能正确广播到所有激活实体
- 只有激活的 Capability 会接收事件
- 异常不会导致系统崩溃
- 编译通过，无运行时错误

---

### Phase 4: World 集成 🔧

**预估工时**: 1-2 小时

- [ ] **P4.1**: 在 `World` 中添加 `GlobalEventQueue` 字段
- [ ] **P4.2**: 在 `World` 构造函数中初始化 `GlobalEventQueue`
- [ ] **P4.3**: 修改 `World.Update()` 调用 `CapabilitySystem.ProcessEntityEvents()`
- [ ] **P4.4**: 在 `OnEntityDestroyed()` 中调用 `entity.ClearEventQueue()`
- [ ] **P4.5**: 在 `Reset()` 中调用 `GlobalEventQueue.ClearAll()` 和清空所有实体事件

**验收标准**：
- World 正确初始化全局事件队列
- 实体销毁时个体事件被清理
- World 重置时所有事件被清理
- 编译通过，无运行时错误

---

### Phase 5: 测试和验证 ✅

**预估工时**: 2-3 小时

#### 5.1 单元测试

- [ ] **P5.1.1**: 测试 `Entity.EventQueue` 基本功能
  - 入队、出队、清理
  - 延迟创建正确性
- [ ] **P5.1.2**: 测试 `GlobalEventQueue` 基本功能
  - 入队、出队、清理
- [ ] **P5.1.3**: 测试 Capability 事件注册
  - `RegisterEventHandlers` 被正确调用
  - 映射正确建立

#### 5.2 集成测试

- [ ] **P5.2.1**: 测试面向个体事件
  - 发布个体事件 → 只有目标实体处理
- [ ] **P5.2.2**: 测试面向全体事件
  - 发布全体事件 → 所有激活实体处理
- [ ] **P5.2.3**: 测试混合场景
  - 同时有个体和全体事件
  - 同一 Capability 处理多种事件类型
- [ ] **P5.2.4**: 测试生命周期
  - 实体销毁时个体事件清理
  - World 重置时所有事件清理
  - Capability 停用时不接收事件

#### 5.3 性能测试

- [ ] **P5.3.1**: 测试个体事件性能（100实体，1000事件）
- [ ] **P5.3.2**: 测试全体事件性能（100实体，10全局事件）
- [ ] **P5.3.3**: 测试装箱拆箱开销
- [ ] **P5.3.4**: 对比旧 EventSystem 性能

**验收标准**：
- 所有测试通过
- 性能满足要求（个体事件 < 0.01ms/事件，全体事件可接受）
- 无内存泄漏
- 无竞争条件（如果支持多线程）

---

## 依赖系统状态

| 系统 | 状态 | 说明 |
|------|------|------|
| CapabilitySystem | ✅ 已存在 | 需要修改注册流程 |
| Capability<T> | ✅ 已存在 | 需要扩展事件处理能力 |
| World | ✅ 已存在 | 需要集成 GlobalEventQueue |
| Entity | ✅ 已存在 | 需要添加 EventQueue 字段 |

---

## 技术债务

- ⚠️ **装箱拆箱性能**: 使用 `object` 和 `DynamicInvoke` 有一定开销，后续可考虑使用 `SourceGenerator` 或 `Expression` 优化
- ⚠️ **反射调用**: `DynamicInvoke` 性能较差，如果成为瓶颈需要优化
- 📝 **文档更新**: 需要更新 Capability 开发指南，说明如何使用事件处理
- 📝 **性能优化**: 可选：为常驻事件的实体维护脏标记，避免遍历所有实体

---

## 关键决策

| 决策 | 理由 |
|------|------|
| 双模式（个体+全体） | 灵活应对不同场景，性能和功能兼顾 |
| 个体事件存实体上 | 数据局部性好，自动生命周期，性能优 |
| 全体事件用全局队列 | 适合广播场景，避免重复存储 |
| 使用静态声明而非 Tick 中拉取 | 声明式更清晰，预处理性能更优 |
| 使用装箱/拆箱 | 简化实现，性能开销可接受 |
| 处理函数第一个参数是 Entity | 明确操作目标，避免闭包捕获 |
| 集中调度而非分散消费 | 统一管理，更易调试和优化 |
| 与 EventSystem 共存（双轨制） | 平滑迁移，各司其职 |

---

## 验收标准

### 功能完整性
- ✅ 个体事件可以正确入队到目标实体
- ✅ 全体事件可以正确入队到全局队列
- ✅ 事件可以根据类型分发到对应 Capability
- ✅ 只有激活的 Capability 接收事件
- ✅ Capability 销毁时自动停止接收事件
- ✅ 实体销毁时清理个体事件队列
- ✅ World 重置时清理所有事件

### 性能要求
- ✅ 个体事件：1000 次处理 < 10ms（单线程）
- ✅ 全体事件：100 个实体同时处理 10 个全局事件 < 50ms
- ✅ 无内存泄漏
- ✅ 装箱开销可接受（< 5% 总开销）

### 代码质量
- ✅ 编译通过，无警告
- ✅ 单元测试覆盖核心逻辑
- ✅ 代码符合项目规范
- ✅ 有清晰的注释和日志
- ✅ 处理函数签名正确（第一个参数是 Entity）

---

## 相关文档

- [事件队列系统设计](../事件/Event-Queue-System%20事件队列系统.md) - 技术方案（双模式）
- [受击与击退开发进展](../../02-CombatSystem%20战斗系统/_status%20开发进展/Hit-Reaction-Progress%20受击与击退开发进展.md) - 第一个应用场景
- [ECC系统](../ECC/ECC-System%20ECC结构说明.md) - 基础架构
